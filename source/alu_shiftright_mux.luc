module alu_shiftright_mux (
    input a, //a[15]
    input alufn1, // selector
    output out
  ) {

  always {
    case(alufn1){
      b0:
        out = b0;
      b1:
        out = a;
      default:
        out = b0;
    }
  }
}